import { isNotEmpty, isString } from 'class-validator';

class CreateUserDto {
  // @isString()
  // @isNotEmpty()
  // title: string;
  // author: string;
  // publisher: string;
  // year: string;
  // page_number: number;
}

// export default new CreateUserDto();
